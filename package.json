{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap --use-workspaces && yarn create-dataset",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^3.19.0"
  }
}
